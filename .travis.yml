dist: trusty
os: linux

language: node_js
node_js:
  - '14'

branches:
  only:
  - master
  - development

addons:
  chrome: stable

cache:
  directories:
    - ./text-adventure-sama/node_modules

install:
  - npm install

script:
  - cd ./text-adventure-sama/
  - ng lint
  - npm run test-ci