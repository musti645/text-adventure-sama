dist: trusty
os: linux

language: node_js
node_js:
  - '12'

branches:
  only:
  - master
  - development

addons:
  apt:
    sources:
      - google-chrome
    packages:
      - google-chrome-stable

cache:
  directories:
    - ./node_modules

install:
  - npm install

script:
  - ng lint
  - npm test-ci