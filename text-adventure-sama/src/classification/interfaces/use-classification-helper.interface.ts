export interface IUseClassificationHelper {
    /**
     * returns an array of strings, that are used to increase the classification score of USE type inputs
     */
    getUseClassificationStrings(): string[];
}